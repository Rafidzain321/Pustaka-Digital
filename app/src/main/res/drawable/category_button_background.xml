<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Saat dipilih -->
    <item android:drawable="@color/primary_purple" android:state_selected="true"/>
    <!-- Saat tidak dipilih -->
    <item android:drawable="@android:color/white"/>
</selector>
